# EventBus — Sanctuary autoload
# Central signal hub for cross-system communication (Harmony backbone pattern).
extends Node

signal creature_selected(genome)
signal breeding_completed(offspring)
signal stat_view_requested(genome)
